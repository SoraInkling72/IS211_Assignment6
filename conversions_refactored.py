import conversions

def fromUnit:
    '''convert one unit of measurement into another'''
    if not (kelvins = kelvins):
        raise ImproperCalculationError ('formula is incorrect')
    if not (celsius = celsius):
        raise ImproperCalculationError ('formula is incorrect')
    if not (fahrenheit = fahrenheit):
        raise ImproperCalculationError ('formula is incorrect')

def toUnit:


def value:
