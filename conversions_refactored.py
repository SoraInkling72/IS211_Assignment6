import conversions
import tests

def fromUnit:
    '''convert one unit of measurement into another'''
    if not (kelvins = kelvins):
        raise ImproperCalculationError ('formula is incorrect')
    if not (celsius = celsius):
        raise ImproperCalculationError ('formula is incorrect')
    if not (fahrenheit = fahrenheit):
        raise ImproperCalculationError ('formula is incorrect')
    if not (meters = meters)
        raise ImproperCalculationError ('formula is incorrect')
    if not (yards = yards)
        raise ImproperCalculationError('formula is incorrect')
    if not (miles = miles)
        raise ImproperCalculationError('formula is incorrect')
    if not (miles = kelvin)
        raise ConversionNotPossible ('unit not compatible')
    if not (miles = kelvin)
        raise ConversionNotPossible ('unit not compatible')
    if not (miles = fahrenheit)
        raise ConversionNotPossible ('unit not compatible')
    if not (miles = celsius)
        raise ConversionNotPossible ('unit not compatible')
    if not (meters = kelvin)
        raise ConversionNotPossible ('unit not compatible')
    if not (meters = fahrenheit)
        raise ConversionNotPossible ('unit not compatible')
    if not (meters = celsius)
        raise ConversionNotPossible ('unit not compatible')
    if not (yards = kelvin)
        raise ConversionNotPossible ('unit not compatible')
    if not (yards = fahrenheit)
        raise ConversionNotPossible ('unit not compatible')
    if not (yards = celsius)
        raise ConversionNotPossible ('unit not compatible')

def toUnit:
    if not (kelvins = kelvins):
        raise ImproperCalculationError ('formula is incorrect')
    if not (celsius = celsius):
        raise ImproperCalculationError ('formula is incorrect')
    if not (fahrenheit = fahrenheit):
        raise ImproperCalculationError ('formula is incorrect')
    if not (meters = meters)
        raise ImproperCalculationError ('formula is incorrect')
    if not (yards = yards)
        raise ImproperCalculationError('formula is incorrect')
    if not (miles = miles)
        raise ImproperCalculationError('formula is incorrect')
    if not (miles = kelvin)
        raise ConversionNotPossible ('unit not compatible')
    if not (miles = fahrenheit)
        raise ConversionNotPossible ('unit not compatible')
    if not (miles = celsius)
        raise ConversionNotPossible ('unit not compatible')
    if not (meters = kelvin)
        raise ConversionNotPossible ('unit not compatible')
    if not (meters = fahrenheit)
        raise ConversionNotPossible ('unit not compatible')
    if not (meters = celsius)
        raise ConversionNotPossible ('unit not compatible')
    if not (yards = kelvin)
        raise ConversionNotPossible ('unit not compatible')
    if not (yards = fahrenheit)
        raise ConversionNotPossible ('unit not compatible')
    if not (yards = celsius)
        raise ConversionNotPossible ('unit not compatible')

def value:
